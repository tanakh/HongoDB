module Database.HongoDB.HashFile (
  ) where

{-
newtype HashFile m a =
  HashFile { unHashFile :: ReaderT HashImpl m a }
  deriving (Monad, MonadIO, MonadTrans, Functor, Applicative)

--instance DB (HashFile m) where
--  accept :: 

-}
